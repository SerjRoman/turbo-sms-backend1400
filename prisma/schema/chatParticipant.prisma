
model ChatParticipant {
    chat   Chat @relation("participants",fields: [chatId], references: [id])
    chatId Int
    user     User   @relation("participants",fields: [userId], references: [id])
    userId   Int

    @@id([chatId, userId])
}