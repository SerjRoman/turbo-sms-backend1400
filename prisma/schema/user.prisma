model User {
  id         Int      @id @default(autoincrement())
  username   String
  name       String
  surname    String
  avatar     String?
  lastSeenAt DateTime
  password   String
  email      String
}
