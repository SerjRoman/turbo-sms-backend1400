model Chat {
  id           Int               @id @default(autoincrement())
  createdAt    DateTime          @default(now())
  updatedAt    DateTime          @updatedAt
  messages     Message[]         @relation(name: "mainChat")
  messageLast  Message?          @relation(name: "messageLast")
  participants ChatParticipant[]
}

//  messages Message[]
//  lastMessage Message
//  participants ChatParticipant
