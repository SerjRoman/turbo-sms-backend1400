model Message {
  id        Int      @id @default(autoincrement())
  type      String
  text      String?
  mediaUrl  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// sendler 
// ChatAsLastMessage
// Chat
